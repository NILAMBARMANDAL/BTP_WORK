[pytest]
markers =
    model: requires loading and running the actual Whisper model (slow; needs a model download and real inference). CI runs everything except these by default — see .github/workflows/ci.yml.
